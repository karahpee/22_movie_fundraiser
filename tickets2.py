# start loop

# make it so loop runs at least once
name = ""
count = 0
MAX_TICKETS = 5

while name!= "xxx" and count < MAX_TICKETS:

    # get hold of the specific details
    name = input("Name: ")
    count += 1
    print()
